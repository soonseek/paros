# Changelog - paros BMAD 개선사항

## 2025-01-21 - 관리자 설정 및 다크모드 추가

### 주요 개선사항

#### 1. 관리자 설정 기능 ✨
- **데이터베이스 기반 설정 관리**
  - 새로운 `SystemSetting` 테이블 추가
  - AI API 키를 데이터베이스에 암호화 저장
  - 관리자 페이지에서 실시간 설정 변경 가능

- **관리자 설정 페이지** (`/admin/settings`)
  - AI 제공자 선택 (Upstage Solar, OpenAI GPT, Anthropic Claude)
  - API 키 관리 (암호화 저장)
  - 관리자 권한 사용자만 접근 가능

- **보안 강화**
  - API 키 AES 암호화 저장
  - JWT_SECRET을 암호화 키로 재사용
  - 관리자 권한 체크 (RBAC)

#### 2. 다크모드 지원 🌙
- **테마 시스템**
  - 라이트 / 다크 / 시스템 모드 지원
  - `ThemeContext`로 전역 테마 관리
  - localStorage에 사용자 선택 저장

- **UI 개선**
  - 모든 페이지에 다크모드 스타일 적용
  - 테마 토글 버튼 (헤더에 배치)
  - 시스템 테마 자동 감지

#### 3. 모바일 최적화 📱
- **반응형 디자인**
  - Tailwind breakpoints 활용 (sm, md, lg)
  - 모바일 우선 레이아웃
  - 터치 친화적 인터페이스

- **개선된 네비게이션**
  - 헤더 flex-wrap으로 모바일 대응
  - 드롭다운 메뉴 위치 최적화

#### 4. 데이터베이스 설정
- **PostgreSQL 설치 및 구성**
  - PostgreSQL 15 설치
  - paros 데이터베이스 생성
  - Prisma 마이그레이션 완료

### 기술 스택 변경

#### 추가된 의존성
```json
{
  "crypto-js": "^4.2.0",
  "@types/crypto-js": "^4.2.2"
}
```

#### 새로운 파일
- `/src/contexts/ThemeContext.tsx` - 테마 관리 컨텍스트
- `/src/components/theme-toggle.tsx` - 테마 토글 컴포넌트
- `/src/server/lib/encryption.ts` - 암호화 유틸리티
- `/src/server/services/settings-service.ts` - 설정 관리 서비스
- `/src/server/api/routers/settings.ts` - 설정 API 라우터
- `/src/server/ai/config.ts` - AI 설정 통합 관리
- `/src/pages/admin/settings.tsx` - 관리자 설정 페이지

#### 수정된 파일
- `/src/pages/_app.tsx` - ThemeProvider 추가
- `/src/pages/dashboard/index.tsx` - 다크모드 스타일, 관리자 메뉴 추가
- `/src/pages/login.tsx` - 다크모드 스타일, 모바일 최적화
- `/src/server/api/root.ts` - settings 라우터 추가
- `/prisma/schema.prisma` - SystemSetting 모델 추가

### 사용 방법

#### 관리자 설정
1. 관리자 권한(ADMIN)으로 로그인
2. 헤더의 설정 아이콘 클릭 또는 `/admin/settings` 접속
3. AI 제공자 선택 및 API 키 입력
4. 저장 버튼 클릭

#### 다크모드
- 헤더의 테마 토글 버튼 클릭
- 라이트 → 다크 → 시스템 순환

### 환경 변수

기존 환경 변수는 여전히 사용 가능하며, 데이터베이스 설정이 우선순위를 가집니다:

```env
# AI Provider (DB 설정이 없을 경우 사용)
AI_PROVIDER=upstage
UPSTAGE_API_KEY=your-key
OPENAI_API_KEY=your-key
ANTHROPIC_API_KEY=your-key

# JWT Secret (암호화에도 사용)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
```

### 데이터베이스 마이그레이션

```bash
# 마이그레이션 적용
npx prisma migrate deploy

# 또는 개발 모드
npx prisma migrate dev
```

### 다음 단계

- [ ] 이메일 서비스 설정 페이지 추가
- [ ] 설정 변경 감사 로그
- [ ] 설정 백업/복원 기능
- [ ] 다국어 지원 확장
- [ ] PWA 지원 (모바일 앱처럼 설치)

---

**개발자**: E1 AI Agent
**날짜**: 2025-01-21
