# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Application
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# Database (PostgreSQL - Neon / Netlify DB)
#
# Local Development:
# Get your connection string from https://console.neon.tech/
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/paros?schema=public"
#
# Netlify Deployment:
# When using Netlify DB, DATABASE_URL is automatically set by @netlify/neon package.
# No manual configuration needed - Netlify creates and manages the database.
#
# Available environment variables after Netlify DB setup:
# - DATABASE_URL: Primary database connection string (auto-set by Netlify)
# - NETLIFY_DB_PGHOST: Database host
# - NETLIFY_DB_PGDATABASE: Database name
# - NETLIFY_DB_PGUSER: Database user
# - NETLIFY_DB_PGPASSWORD: Database password
# - NEON_DATABASE_URL: Alternative connection string

# Local development (comment out for Netlify deployment)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/paros?schema=public"

# JWT Secret (required for authentication)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters-long"

# Email Service (Nodemailer - Optional)
# Development: Logs to console (no configuration needed)
# Production: Configure your SMTP server
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
EMAIL_FROM="paros BMAD <noreply@paros-bmad.com>"

# Alternatively, use Resend (recommended for production)
# Get your API key from https://resend.com/api-keys
# RESEND_API_KEY="re_xxxxxxxxx"

# ============================================================================
# Story 4.1: AI Classification Configuration
# ============================================================================

# Choose an AI provider for transaction classification
# Options: upstage, openai, anthropic
# Default: upstage
AI_PROVIDER="upstage"

# Upstage Solar API (Recommended for Korean)
# Get your API key from: https://console.upstage.ai
UPSTAGE_API_KEY="your-upstage-api-key"

# OpenAI GPT API
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key"

# Anthropic Claude API
# Get your API key from: https://console.anthropic.com
ANTHROPIC_API_KEY="your-anthropic-api-key"
